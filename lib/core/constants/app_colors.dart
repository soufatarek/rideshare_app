import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF2B8CEE); // Stitch Blue
  static const Color secondary = Color(0xFF121212); // Dark Background
  static const Color accent = Color(0xFF276EF1); // Uber Blue (Keep as accent)
  static const Color background = Color(0xFF1E1E1E); // Dark Surface
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFB0B0B0);
  static const Color error = Color(0xFFCF6679); // Material Dark Error
}
